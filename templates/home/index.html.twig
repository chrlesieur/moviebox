{% extends 'layoutHome.html.twig' %}
{% form_theme form 'bootstrap_4_horizontal_layout.html.twig' %}

{% block title %} Moviebox, l'application des cinéphiles !{% endblock %}

{% block body %}


<div class="row justify-content-center">
    <h1 class="title">Bienvenue {{ user.firstname }} {{ user.lastname }} !  </h1>
</div>
    <div class="container">
        <div class="row">
            <div class="col-12">

                {{ form_start(form) }}

                {{ form_row ( form.field_name ) }}

                <div class="text-center pb-3">
                    <button type="submit" class="btn btn-primary">Rechercher</button>
                </div>

                {{ form_end(form) }}


                    {{ form_start(form) }}

                    {{ form_row ( form.title ) }}

                    {{ form_row ( form.year ) }}

                    {{ form_row ( form.runtime ) }}

                    {{ form_row ( form.genre ) }}

                    {{ form_row ( form.director ) }}

                    {{ form_row ( form.actors ) }}

                    {{ form_row ( form.resume ) }}

                    {{ form_row ( form.country) }}

                    {{ form_row ( form.poster) }}

                    <p> <a href="" /></p>

                    <div class="text-center">
                        <button type="submit" class="btn btn-primary">Enregistrer ma recherche</button>
                    </div>

                    {{ form_end(form) }}

                </div>
    </div>
        </div>

{% endblock %}
