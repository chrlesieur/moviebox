<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="{{ path('app_logout') }}">Se déconnecter</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
        <ul class="navbar-nav">
            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link {% if app.request.get('_route') == 'user_index' %} active {% endif %}" href="{{ path('user_index') }}">Gestion des utilisateurs</a>
                </li>
            {% endif %}
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <li class=" title nav-item pl-5 pt-2">
                Bienvenue {{ app.user.firstName }} {{ app.user.lastName }}
            </li>
            {% endif %}
            <li class="nav-item pl-5">
                <a class="nav-link  {% if app.request.get('_route') == 'movie_new' %} active {% endif %}" href="{{ path('search_movie') }}">Recherche de film</a>
            </li>
            <li class="nav-item pl-5">
                <a class="nav-link  {% if app.request.get('_route') == 'movie_index' %} active {% endif %}" href="{{ path('movie_index') }}">Ma Moviebox</a>
            </li>
            <li class="nav-item pl-5">
                <a class="nav-link  {% if app.request.get('_route') == 'contact' %} active {% endif %}" href="{{ path('contact') }}">Nous contacter</a>
            </li>
        </ul>
    </div>
</nav>